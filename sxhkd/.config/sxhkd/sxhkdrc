# super + ctrl + shift + space + Escape + Tab + alt

super + Return
	alacritty


# Browser

super + b
    brave

super + shift + b
    brave --incognito

super + o
    opera

super + f
    firejail firefox

super + c
    google-chrome-stable

super + v
    code

super + shift + d
    code & google-chrome-stable

super + m
    chromium --class="muskaat"

super + s
    qutebrowser

super + p
    pamac-manager

super + e
    pcmanfm

super + t
    thunderbird

super + ctrl + y 
    flameshot gui -p ~/Screenshots

super + ctrl + c
    flameshot full -p ~/Screenshots

super + shift + a
    alacritty --class scratch -e ncmpcpp

super + shift + m
    alacritty --class scratch -e neomutt

super + shift + f
    alacritty --class scratch -e ranger

super + shift + n
    alacritty --class scratch -e newsboat

super + shift + c
    pcmanfm ~/Screenshots

super + shift + Return
    alacritty --class floating

super + shift + p
    alacritty --class floating -e ~/.scripts/tmuxstarter.sh

super + shift + y
    ~/.scripts/resetscreen.sh

